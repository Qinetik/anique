---
layout: ../../layouts/MDXLayout.astro
title: Icon | Anique
---
import {Accordion, Anique, Card, Headline, Icon, Row} from "@qinetik/anique";
import MenuIcon from "../../solid/icons/Menu.js"

<Headline>Icon</Headline>

Icon is a simple component to display a svg icon, The svg icon is the child passed to Icon component

<Card>
    <Icon>
        <MenuIcon />
    </Icon>
</Card>
<br />
<Accordion title={"Code"} defaultExpanded={false} client:load>
```typescript jsx
    <Icon>
        <MenuIcon />
    </Icon>
```
</Accordion>

At the moment, You can pass size and fill

<Card>
    <Row>
        <Icon
            fill={Anique.colors.success}
            size={2}
        >
            <MenuIcon/>
        </Icon>
        <Icon
            fill={Anique.colors.error}
            size={2}
        >
            <MenuIcon/>
        </Icon>
    </Row>
</Card>
<br />
<Accordion title={"Code"} defaultExpanded={false} client:load>
    ```typescript jsx
    <Row>
        <Icon
            fill={Anique.colors.success}
            size={2}
        >
            <MenuIcon/>
        </Icon>
        <Icon
            fill={Anique.colors.error}
            size={2}
        >
            <MenuIcon/>
        </Icon>
    </Row>
    ```
</Accordion>
