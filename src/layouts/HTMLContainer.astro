
---
import {AniqueThemeDark, AniqueThemeLight} from "@qinetik/anique/theme/AniqueThemes";
import {Anique} from "@qinetik/anique/theme/Theme";
import {AniqueGlobalStyling} from "@qinetik/anique/theme/AniqueThemes";

export interface Props {
    initialTheme: string
    title: string
}

const {
    initialTheme,
    title
} = Astro.props

const onBg = Anique.colors.onBg

---

<html class={initialTheme}>
    <head>
        <title>{title}</title>
        <AniqueThemeDark containerCssSelector={".dark"}/>
        <AniqueThemeLight containerCssSelector={".light"}/>
        <AniqueGlobalStyling />
    </head>
    <body>
        <slot/>
    </body>
</html>

<style is:global>

    :root.light {
        color-scheme : light;
    }

    :root.dark {
        color-scheme : dark;
    }

    body {
        margin: 0;
    }

    .light .display-in-dark {
        display:none !important;
    }
    .dark .display-in-light {
        display:none !important;
    }

</style>